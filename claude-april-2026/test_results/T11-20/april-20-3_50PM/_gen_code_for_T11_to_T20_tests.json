[
  {
    "T11": "def process_sf_film_query(gdf):\n    import pandas as pd\n    import numpy as np\n    import geopandas as gpd\n\n    def clean_column_data(series):\n        stringified = series.astype(str)\n        exclude = (\n            series.isna() |\n            (stringified.str.strip() == '') |\n            (stringified.str.lower().isin(['none', 'nan', 'null']))\n        )\n        return series[~exclude]\n\n    try:\n        gdf_copy = gdf.copy()\n        task_results = {}\n\n        # \u2500\u2500 Task t1: retrieve films by zachary shedd or nicholas meyer \u2500\u2500\n        director_str = gdf_copy['Director'].astype(str)\n        director_valid = (\n            gdf_copy['Director'].notna() &\n            director_str.str.strip().ne('') &\n            ~director_str.str.lower().isin(['none', 'nan', 'null'])\n        )\n\n        mask_shedd = director_valid & director_str.str.contains('zachary shedd', case=False, na=False)\n        mask_meyer = director_valid & director_str.str.contains('nicholas meyer', case=False, na=False)\n        \n        combined_mask = mask_shedd | mask_meyer\n        t1_matched_rows = gdf_copy.loc[combined_mask]\n        t1_text_view = t1_matched_rows.drop_duplicates(subset=['Title', 'Year'])\n\n        task_results['t1'] = {\n            'matched_rows': t1_matched_rows,\n            'text_view': t1_text_view,\n            'result': {\n                'records': t1_text_view.to_dict('records'),\n                'count': len(t1_text_view),\n            }\n        }\n\n        # \u2500\u2500 Assemble final result \u2500\u2500\n        result = {\n            'data': task_results['t1']['result']['records'],\n            'summary': f'Found {len(t1_text_view)} films directed by Zachary Shedd or Nicholas Meyer.',\n            'metadata': {\n                'query_type': 'retrieve',\n                'task_count': 1,\n                'task_ids': ['t1'],\n            }\n        }\n\n        return result\n\n    except Exception as e:\n        return {\n            'data': None,\n            'summary': f'Error processing query: {str(e)}',\n            'metadata': {'error': str(e), 'error_type': type(e).__name__}\n        }"
  },
  {
    "T12": "def process_sf_film_query(gdf):\n    import pandas as pd\n    import numpy as np\n    import geopandas as gpd\n\n    def clean_column_data(series):\n        stringified = series.astype(str)\n        exclude = (\n            series.isna() |\n            (stringified.str.strip() == '') |\n            (stringified.str.lower().isin(['none', 'nan', 'null']))\n        )\n        return series[~exclude]\n\n    try:\n        gdf_copy = gdf.copy()\n        task_results = {}\n\n        # \u2500\u2500 Task t1: retrieve the film called time after time \u2500\u2500\n        title_str = gdf_copy['Title'].astype(str)\n        mask_t1 = (\n            gdf_copy['Title'].notna() &\n            title_str.str.strip().ne('') &\n            ~title_str.str.lower().isin(['none', 'nan', 'null']) &\n            title_str.str.contains('time after time', case=False, na=False)\n        )\n        t1_matched_rows = gdf_copy.loc[mask_t1]\n        t1_text_view = t1_matched_rows.drop_duplicates(subset=['Title', 'Year'])\n\n        task_results['t1'] = {\n            'matched_rows': t1_matched_rows,\n            'text_view': t1_text_view,\n            'result': {\n                'records': t1_text_view.to_dict('records'),\n                'count': len(t1_text_view),\n            }\n        }\n\n        # \u2500\u2500 Assemble final result \u2500\u2500\n        result = {\n            'data': task_results['t1']['result'],\n            'summary': f\"Found {len(task_results['t1']['text_view'])} film(s) matching 'time after time'.\",\n            'metadata': {\n                'query_type': 'retrieve',\n                'task_count': 1,\n                'task_ids': ['t1'],\n            }\n        }\n\n        return result\n\n    except Exception as e:\n        error_result = {\n            'data': None,\n            'summary': f\"Error processing query: {str(e)}\",\n            'metadata': {\n                'error': str(e),\n                'error_type': type(e).__name__\n            }\n        }\n        return error_result"
  },
  {
    "T13": "def process_sf_film_query(gdf):\n    import pandas as pd\n    import numpy as np\n    import geopandas as gpd\n\n    def clean_column_data(series):\n        stringified = series.astype(str)\n        exclude = (\n            series.isna() |\n            (stringified.str.strip() == '') |\n            (stringified.str.lower().isin(['none', 'nan', 'null']))\n        )\n        return series[~exclude]\n\n    try:\n        gdf_copy = gdf.copy()\n        task_results = {}\n\n        # \u2500\u2500 Task t1: retrieve films with boys in the title \u2500\u2500\n        title_str = gdf_copy['Title'].astype(str)\n        mask_t1 = (\n            gdf_copy['Title'].notna() &\n            title_str.str.strip().ne('') &\n            ~title_str.str.lower().isin(['none', 'nan', 'null']) &\n            title_str.str.contains('boys', case=False, na=False)\n        )\n        t1_matched_rows = gdf_copy.loc[mask_t1]\n        t1_text_view = t1_matched_rows.drop_duplicates(subset=['Title', 'Year'])\n\n        task_results['t1'] = {\n            'matched_rows': t1_matched_rows,\n            'text_view': t1_text_view,\n            'result': {\n                'records': t1_text_view.to_dict('records'),\n                'count': len(t1_text_view),\n            }\n        }\n\n        # \u2500\u2500 Assemble final result \u2500\u2500\n        result = {\n            'data': task_results['t1']['result'],\n            'summary': f\"Found {len(task_results['t1']['text_view'])} films with 'boys' in the title.\",\n            'metadata': {\n                'query_type': 'retrieve',\n                'task_count': 1,\n                'task_ids': ['t1'],\n            }\n        }\n\n        return result\n\n    except Exception as e:\n        return {\n            'data': None,\n            'summary': f\"Error processing query: {str(e)}\",\n            'metadata': {'error': str(e), 'error_type': type(e).__name__}\n        }"
  },
  {
    "T14": "def process_sf_film_query(gdf):\n    import pandas as pd\n    import numpy as np\n    import geopandas as gpd\n\n    def clean_column_data(series):\n        stringified = series.astype(str)\n        exclude = (\n            series.isna() |\n            (stringified.str.strip() == '') |\n            (stringified.str.lower().isin(['none', 'nan', 'null']))\n        )\n        return series[~exclude]\n\n    try:\n        gdf_copy = gdf.copy()\n        task_results = {}\n\n        # \u2500\u2500 Task t1: retrieve films directed by michel brezis \u2500\u2500\n        director_str = gdf_copy['Director'].astype(str)\n        mask_director = (\n            gdf_copy['Director'].notna() &\n            director_str.str.strip().ne('') &\n            ~director_str.str.lower().isin(['none', 'nan', 'null']) &\n            director_str.str.contains('michel brezis', case=False, na=False)\n        )\n        t1_matched_rows = gdf_copy.loc[mask_director]\n        t1_text_view = t1_matched_rows.drop_duplicates(subset=['Title', 'Year'])\n\n        task_results['t1'] = {\n            'matched_rows': t1_matched_rows,\n            'text_view': t1_text_view,\n            'result': {\n                'records': t1_text_view.to_dict('records'),\n                'count': len(t1_text_view),\n            }\n        }\n\n        # \u2500\u2500 Assemble final result \u2500\u2500\n        result = {\n            'data': task_results['t1']['result']['records'],\n            'summary': f\"Found {len(task_results['t1']['text_view'])} films directed by michel brezis.\",\n            'metadata': {\n                'query_type': 'retrieve',\n                'task_count': 1,\n                'task_ids': ['t1'],\n            }\n        }\n\n        return result\n\n    except Exception as e:\n        return {\n            'data': None,\n            'summary': f\"Error processing query: {str(e)}\",\n            'metadata': {'error': str(e), 'error_type': type(e).__name__}\n        }"
  },
  {
    "T15": "def process_sf_film_query(gdf):\n    import pandas as pd\n    import numpy as np\n    import geopandas as gpd\n\n    def clean_column_data(series):\n        stringified = series.astype(str)\n        exclude = (\n            series.isna() |\n            (stringified.str.strip() == '') |\n            (stringified.str.lower().isin(['none', 'nan', 'null']))\n        )\n        return series[~exclude]\n\n    try:\n        gdf_copy = gdf.copy()\n        task_results = {}\n\n        # \u2500\u2500 Task t1: retrieve films on larkin st \u2500\u2500\n        mask_t1 = gdf_copy['Locations'].astype(str).str.contains('larkin st', case=False, na=False)\n        t1_matched_rows = gdf_copy.loc[mask_t1]\n        t1_text_view = t1_matched_rows.drop_duplicates(subset=['Title', 'Year'])\n\n        task_results['t1'] = {\n            'matched_rows': t1_matched_rows,\n            'text_view': t1_text_view,\n            'result': {\n                'records': t1_text_view.to_dict('records'),\n                'count': len(t1_text_view),\n            }\n        }\n\n        # \u2500\u2500 Assemble final result \u2500\u2500\n        result = {\n            'data': task_results['t1']['result']['records'],\n            'summary': f\"Found {len(task_results['t1']['text_view'])} films filmed on Larkin St.\",\n            'metadata': {\n                'query_type': 'retrieve',\n                'task_count': 1,\n                'task_ids': ['t1'],\n            }\n        }\n\n        with open('code_gen_result.log', 'a', encoding='utf-8', errors='replace') as f:\n            f.write('=' * 50 + '\\n')\n            f.write('Query Result\\n')\n            f.write('-' * 50 + '\\n')\n            f.write(str(result['data']) + '\\n')\n            f.write(f\"Summary: {result['summary']}\\n\")\n            f.write('=' * 50 + '\\n\\n')\n\n        return result\n\n    except Exception as e:\n        error_result = {\n            'data': None,\n            'summary': f\"Error processing query: {str(e)}\",\n            'metadata': {\n                'error': str(e),\n                'error_type': type(e).__name__\n            }\n        }\n        with open('code_gen_result.log', 'a', encoding='utf-8', errors='replace') as f:\n            f.write('=' * 50 + '\\n')\n            f.write(f'ERROR: {str(e)}\\n')\n            f.write('=' * 50 + '\\n\\n')\n        return error_result"
  },
  {
    "T16": "def process_sf_film_query(gdf):\n    import pandas as pd\n    import numpy as np\n    import geopandas as gpd\n\n    def clean_column_data(series):\n        stringified = series.astype(str)\n        exclude = (\n            series.isna() |\n            (stringified.str.strip() == '') |\n            (stringified.str.lower().isin(['none', 'nan', 'null']))\n        )\n        return series[~exclude]\n\n    try:\n        gdf_copy = gdf.copy()\n        task_results = {}\n\n        # \u2500\u2500 Task t1: retrieve films with no listed director \u2500\u2500\n        # Predicate: Director == null\n        # We define 'no listed director' as null, empty, or stringy-nulls.\n        director_str = gdf_copy['Director'].astype(str)\n        mask_t1 = (\n            gdf_copy['Director'].isna() |\n            (director_str.str.strip() == '') |\n            (director_str.str.lower().isin(['none', 'nan', 'null']))\n        )\n        \n        t1_matched_rows = gdf_copy.loc[mask_t1]\n        t1_text_view = t1_matched_rows.drop_duplicates(subset=['Title', 'Year'])\n\n        task_results['t1'] = {\n            'matched_rows': t1_matched_rows,\n            'text_view': t1_text_view,\n            'result': {\n                'records': t1_text_view.to_dict('records'),\n                'count': len(t1_text_view),\n            }\n        }\n\n        # \u2500\u2500 Assemble final result \u2500\u2500\n        result = {\n            'data': task_results['t1']['result'],\n            'summary': f\"Found {len(task_results['t1']['result']['records'])} films with no listed director.\",\n            'metadata': {\n                'query_type': 'retrieve',\n                'task_count': 1,\n                'task_ids': ['t1'],\n            }\n        }\n\n        return result\n\n    except Exception as e:\n        return {\n            'data': None,\n            'summary': f\"Error processing query: {str(e)}\",\n            'metadata': {'error': str(e), 'error_type': type(e).__name__}\n        }"
  },
  {
    "T17": "def process_sf_film_query(gdf):\n    import pandas as pd\n    import numpy as np\n    import geopandas as gpd\n\n    def clean_column_data(series):\n        stringified = series.astype(str)\n        exclude = (\n            series.isna() |\n            (stringified.str.strip() == '') |\n            (stringified.str.lower().isin(['none', 'nan', 'null']))\n        )\n        return series[~exclude]\n\n    try:\n        gdf_copy = gdf.copy()\n        task_results = {}\n\n        # \u2500\u2500 Task t1: retrieve films directed by nicholas meyer or at coit tower \u2500\u2500\n        # Predicate: (Director == 'nicholas meyer') OR (Locations contains 'coit tower')\n        \n        # Mask for Director (fully guarded)\n        director_str = gdf_copy['Director'].astype(str)\n        mask_director = (\n            gdf_copy['Director'].notna() &\n            director_str.str.strip().ne('') &\n            ~director_str.str.lower().isin(['none', 'nan', 'null']) &\n            director_str.str.contains('nicholas meyer', case=False, na=False)\n        )\n\n        # Mask for Locations (short form)\n        mask_location = gdf_copy['Locations'].astype(str).str.contains('coit tower', case=False, na=False)\n\n        combined_mask = mask_director | mask_location\n        t1_matched_rows = gdf_copy.loc[combined_mask]\n        \n        # response_granularity: location (keep as-is)\n        t1_text_view = t1_matched_rows\n\n        task_results['t1'] = {\n            'matched_rows': t1_matched_rows,\n            'text_view': t1_text_view,\n            'result': {\n                'records': t1_text_view.to_dict('records'),\n                'count': len(t1_text_view),\n            }\n        }\n\n        # \u2500\u2500 Assemble final result \u2500\u2500\n        result = {\n            'data': task_results['t1']['result']['records'],\n            'summary': f\"Found {len(t1_text_view)} filming locations for films directed by nicholas meyer or filmed at coit tower.\",\n            'metadata': {\n                'query_type': 'retrieve',\n                'task_count': 1,\n                'task_ids': ['t1'],\n            }\n        }\n\n        return result\n\n    except Exception as e:\n        return {\n            'data': None,\n            'summary': f\"Error processing query: {str(e)}\",\n            'metadata': {'error': str(e), 'error_type': type(e).__name__}\n        }"
  },
  {
    "T18": "def process_sf_film_query(gdf):\n    import pandas as pd\n    import numpy as np\n    import geopandas as gpd\n\n    def clean_column_data(series):\n        stringified = series.astype(str)\n        exclude = (\n            series.isna() |\n            (stringified.str.strip() == '') |\n            (stringified.str.lower().isin(['none', 'nan', 'null']))\n        )\n        return series[~exclude]\n\n    try:\n        gdf_copy = gdf.copy()\n        task_results = {}\n\n        # \u2500\u2500 Task t1: retrieve films on larkin st \u2500\u2500\n        mask_t1 = gdf_copy['Locations'].astype(str).str.contains('larkin st', case=False, na=False)\n        t1_matched_rows = gdf_copy.loc[mask_t1]\n        t1_text_view = t1_matched_rows.drop_duplicates(subset=['Title', 'Year'])\n\n        task_results['t1'] = {\n            'matched_rows': t1_matched_rows,\n            'text_view': t1_text_view,\n            'result': {\n                'records': t1_text_view.to_dict('records'),\n                'count': len(t1_text_view),\n            }\n        }\n\n        # \u2500\u2500 Task t2: count (depends on t1) \u2500\u2500\n        t2_matched_rows = task_results['t1']['matched_rows']\n        t2_film_view = t2_matched_rows.drop_duplicates(subset=['Title', 'Year'])\n        t2_count = len(t2_film_view)\n\n        task_results['t2'] = {\n            'matched_rows': t2_matched_rows,\n            'text_view': None,\n            'result': {\n                'count': t2_count,\n                'count_basis': 'films',\n            }\n        }\n\n        # \u2500\u2500 Assemble final result \u2500\u2500\n        result = {\n            'data': task_results['t2']['result'],\n            'summary': f\"Found {task_results['t2']['result']['count']} films on Larkin St.\",\n            'metadata': {\n                'query_type': 'retrieve_and_count',\n                'task_count': 2,\n                'task_ids': ['t1', 't2'],\n            }\n        }\n\n        return result\n\n    except Exception as e:\n        return {\n            'data': None,\n            'summary': f\"Error processing query: {str(e)}\",\n            'metadata': {'error': str(e), 'error_type': type(e).__name__}\n        }"
  },
  {
    "T19": "def process_sf_film_query(gdf):\n    import pandas as pd\n    import numpy as np\n    import geopandas as gpd\n\n    def clean_column_data(series):\n        stringified = series.astype(str)\n        exclude = (\n            series.isna() |\n            (stringified.str.strip() == '') |\n            (stringified.str.lower().isin(['none', 'nan', 'null']))\n        )\n        return series[~exclude]\n\n    try:\n        gdf_copy = gdf.copy()\n        task_results = {}\n\n        # \u2500\u2500 Task t1: retrieve filming locations for time after time \u2500\u2500\n        title_str = gdf_copy['Title'].astype(str)\n        mask_t1 = (\n            gdf_copy['Title'].notna() &\n            title_str.str.strip().ne('') &\n            ~title_str.str.lower().isin(['none', 'nan', 'null']) &\n            title_str.str.contains('time after time', case=False, na=False)\n        )\n        t1_matched_rows = gdf_copy.loc[mask_t1]\n        t1_text_view = t1_matched_rows\n\n        task_results['t1'] = {\n            'matched_rows': t1_matched_rows,\n            'text_view': t1_text_view,\n            'result': {\n                'records': t1_text_view.to_dict('records'),\n                'count': len(t1_text_view),\n            }\n        }\n\n        # \u2500\u2500 Task t2: count how many locations are there \u2500\u2500\n        t2_matched_rows = task_results['t1']['matched_rows']\n        t2_count = len(t2_matched_rows)\n\n        task_results['t2'] = {\n            'matched_rows': t2_matched_rows,\n            'text_view': None,\n            'result': {\n                'count': t2_count,\n                'count_basis': 'locations',\n            }\n        }\n\n        result = {\n            'data': {\n                'locations': task_results['t1']['result']['records'],\n                'location_count': task_results['t2']['result']['count']\n            },\n            'summary': f\"Found {task_results['t2']['result']['count']} filming locations for 'time after time'.\",\n            'metadata': {\n                'query_type': 'retrieve_and_count',\n                'task_count': 2,\n                'task_ids': ['t1', 't2'],\n            }\n        }\n\n        return result\n\n    except Exception as e:\n        return {\n            'data': None,\n            'summary': f\"Error processing query: {str(e)}\",\n            'metadata': {'error': str(e), 'error_type': type(e).__name__}\n        }"
  },
  {
    "T20": "def process_sf_film_query(gdf):\n    import pandas as pd\n    import numpy as np\n    import geopandas as gpd\n\n    def clean_column_data(series):\n        stringified = series.astype(str)\n        exclude = (\n            series.isna() |\n            (stringified.str.strip() == '') |\n            (stringified.str.lower().isin(['none', 'nan', 'null']))\n        )\n        return series[~exclude]\n\n    try:\n        gdf_copy = gdf.copy()\n        task_results = {}\n\n        # \u2500\u2500 Task t1: retrieve films by nicholas meyer at the hyatt regency hotel \u2500\u2500\n        # Predicate: Director == 'nicholas meyer' AND Locations contains 'hyatt regency hotel'\n        \n        # Director mask (fully guarded)\n        director_str = gdf_copy['Director'].astype(str)\n        mask_director = (\n            gdf_copy['Director'].notna() &\n            director_str.str.strip().ne('') &\n            ~director_str.str.lower().isin(['none', 'nan', 'null']) &\n            director_str.str.contains('nicholas meyer', case=False, na=False)\n        )\n\n        # Location mask (short form)\n        mask_location = gdf_copy['Locations'].astype(str).str.contains(\n            'hyatt regency hotel', case=False, na=False\n        )\n\n        combined_mask = mask_director & mask_location\n        t1_matched_rows = gdf_copy.loc[combined_mask]\n        t1_text_view = t1_matched_rows.drop_duplicates(subset=['Title', 'Year'])\n\n        task_results['t1'] = {\n            'matched_rows': t1_matched_rows,\n            'text_view': t1_text_view,\n            'result': {\n                'records': t1_text_view.to_dict('records'),\n                'count': len(t1_text_view),\n            }\n        }\n\n        # \u2500\u2500 Assemble final result \u2500\u2500\n        result = {\n            'data': task_results['t1']['result']['records'],\n            'summary': f\"Found {len(task_results['t1']['text_view'])} film(s) directed by nicholas meyer filmed at the hyatt regency hotel.\",\n            'metadata': {\n                'query_type': 'retrieve',\n                'task_count': 1,\n                'task_ids': ['t1'],\n            }\n        }\n\n        return result\n\n    except Exception as e:\n        return {\n            'data': None,\n            'summary': f\"Error processing query: {str(e)}\",\n            'metadata': {'error': str(e), 'error_type': type(e).__name__}\n        }"
  }
]