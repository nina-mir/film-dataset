{
  "code": "import pandas as pd\nimport numpy as np\nimport geopandas as gpd\nfrom shapely.geometry import Point\n\ndef process_sf_film_query(gdf):\n    \"\"\"\n    Execute a read-only query against the SF film locations GeoDataFrame.\n    Returns a standardized result dictionary.\n    \"\"\"\n    def clean_column_data(series):\n        \"\"\"Remove empty/null/whitespace/stringy-null values from a string Series.\n\n        WARNING: Returns a FILTERED series with fewer rows.\n        Use ONLY for final output cleanup, NEVER for building boolean masks.\n        \"\"\"\n        stringified = series.astype(str)\n        exclude = (\n            series.isna() |\n            (stringified.str.strip() == '') |\n            (stringified.str.lower().isin(['none', 'nan', 'null']))\n        )\n        return series[~exclude]\n\n    try:\n        gdf_copy = gdf.copy()\n\n        task_results = {}\n\n        # \u2500\u2500 Task t1 \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n        # Predicate: {\"field\": \"Title\", \"op\": \"==\", \"value\": \"vertigo\", \"type\": \"attribute\"}\n        title_str = gdf_copy['Title'].astype(str)\n        mask_t1_title = (\n            gdf_copy['Title'].notna() &\n            title_str.str.strip().ne('') &\n            ~title_str.str.lower().isin(['none', 'nan', 'null']) &\n            title_str.str.contains('vertigo', case=False, na=False)\n        )\n        t1_matched_rows = gdf_copy.loc[mask_t1_title]\n\n        # response_granularity: \"location\"\n        t1_text_view = t1_matched_rows\n\n        task_results[\"t1\"] = {\n            \"matched_rows\": t1_matched_rows,\n            \"text_view\": t1_text_view,\n            \"result\": {\n                \"records\": t1_text_view.to_dict(\"records\"),\n                \"count\": len(t1_text_view),\n            }\n        }\n\n        # \u2500\u2500 Assemble final result \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n        final_data = task_results[\"t1\"][\"text_view\"]\n        final_summary = f\"Found {len(final_data)} filming locations for 'Vertigo'.\"\n\n        result = {\n            'data': final_data,\n            'summary': final_summary,\n            'metadata': {\n                'query_type': 'retrieve',\n                'task_count': 1,\n                'task_ids': ['t1'],\n            }\n        }\n\n        # \u2500\u2500 Log (UTF-8 safe) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n        with open('code_gen_result.log', 'a', encoding='utf-8', errors='replace') as f:\n            f.write('=' * 50 + '\\n')\n            f.write('Query Result\\n')\n            f.write('-' * 50 + '\\n')\n            if isinstance(result['data'], (pd.DataFrame, gpd.GeoDataFrame)):\n                f.write(result['data'].to_string() + '\\n')\n            else:\n                f.write(str(result['data']) + '\\n')\n            f.write(f\"Summary: {result['summary']}\\n\")\n            f.write('=' * 50 + '\\n\\n')\n\n        return result\n\n    except Exception as e:\n        error_result = {\n            'data': None,\n            'summary': f\"Error processing query: {str(e)}\",\n            'metadata': {\n                'error': str(e),\n                'error_type': type(e).__name__\n            }\n        }\n        with open('code_gen_result.log', 'a', encoding='utf-8', errors='replace') as f:\n            f.write('=' * 50 + '\\n')\n            f.write(f'ERROR: {str(e)}\\n')\n            f.write('=' * 50 + '\\n\\n')\n        return error_result",
  "explanation": "The code defines a function `process_sf_film_query` that takes a GeoDataFrame `gdf` as input. It first creates a copy of the GeoDataFrame to ensure no mutations to the original. A helper function `clean_column_data` is included for output cleanup, but not used for mask building. The code then executes a single task `t1` to retrieve filming locations for 'Vertigo'. It constructs a boolean mask by checking the 'Title' column for case-insensitive containment of 'vertigo', while also guarding against null, empty, or stringy-null values. The `matched_rows` are filtered using this mask. Since the `response_granularity` is 'location', the `text_view` is set to the `matched_rows` directly. Finally, the results are stored in `task_results` and assembled into a standardized output dictionary, including the filtered GeoDataFrame, a summary string, and metadata. Error handling is included to catch exceptions and return a structured error payload."
}